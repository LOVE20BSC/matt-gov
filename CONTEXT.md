# LOVE20BSC 治理术语

## 参与主体

- **BSC 参与主体**：除明确的技术性特殊情况外，所有业务参与主体统一为 `MemberNFT`；`memberId` 是主体身份，钱包地址只作为当前控制者或交易调用者。
- **质押状态归属**：治理质押状态按 `memberId` 归属，不按钱包地址归属；`MemberNFT` 转移时，质押资产、解锁期、解锁申请状态和治理权一并随 NFT 转移，由当前持有人继续操作。
- **质押类型**：同一 `MemberNFT` 可以同时拥有流动性质押和加速质押；流动性质押产生治理权，加速质押不单独产生治理资格，只参与治理激励中的加速分配。
- **加速质押前提**：只有拥有有效流动性质押治理权的 `MemberNFT` 才能增加加速质押。
- **LP 份额账本**：流动性质押使用 `Stake` 内部的标准化 LP 份额和实际可赎回 LP 余额记录，不再通过 SL 凭证表达；治理票使用标准化份额，退出按份额比例赎回当前可用 LP。
- **统一解锁**：流动性质押和加速质押共享同一个解锁申请、等待条件和提取操作，不能单独解锁其中一种。
- **解锁状态转移**：解锁申请的轮次、等待期和到期时间绑定 `memberId`；`MemberNFT` 转移只改变当前控制者，不重置倒计时；待处理期间不能追加质押或融合。
- **融合投票边界**：当前投票轮内，源或目标 `MemberNFT` 任一发生过非零投票都不能融合；历史投票归原 NFT，进入下一轮后不再阻止融合。
- **融合范围**：治理质押融合按 `tokenAddress` 的代币社区隔离，函数必须显式指定社区；同一 `MemberNFT` 的多社区质押分别处理，不跨社区合并。
- **融合与解锁**：源或目标 `MemberNFT` 存在待处理解锁申请时不能融合；双方完成提取后才可再次融合。
- **融合后等待期**：融合后的质押统一采用目标 `MemberNFT` 的 `promisedWaitingPhases`，治理票按合并后的 LP 份额重算。
- **融合权限**：融合按 `tokenAddress` 的代币社区分别执行，只能由同一调用者同时控制源和目标 `MemberNFT`；待解锁或本轮已投票时不能融合。
- **融合历史**：融合前已发生的投票、快照、奖励和事件归原 NFT，源 NFT 身份保留但质押清零。
- **奖励轮次隔离**：治理和行动奖励按社区、轮次及各自领取主体隔离记录；已准备轮次额度冻结，不同轮次可并行铸造。
- **LP 交易基础设施**：BSC 版直接使用 BSC 上与 Uniswap V2 兼容的 PancakeSwap 交易对和路由，不自建 DEX；核心合约只依赖必要的 Factory、Pair 和 Router 接口，并在部署/集成时验证实际接口兼容性。
- **流动性质押手续费**：继续记录流动性质押 LP 对应的交易手续费，在质押退出流程中结算；社区代币部分直接销毁，父币部分通过兼容 Uniswap V2 的 PancakeSwap Router 换成该社区代币后再销毁。每次结算提供按实际被销毁代币汇总的全局累计量和按代币社区拆分的累计量，不再把父币手续费转入子币托底池。
- **发射额度**：按 `tokenAddress + memberId` 累计实际治理激励铸造量；达到当前剩余未铸造量乘以配置比例时，消耗一份额度并自动铸造一枚 `LaunchNFT`，剩余额度随 NFT 转移。
- **治理激励与发射 NFT**：达到额度的 `LaunchNFT` 在治理激励铸造的同一笔交易中原子铸造；奖励和销毁状态必须防止重复结算。
- **发射分配机制**：任何持有 `LaunchNFT` 的钱包或合约都可触发发射；中心化与去中心化只描述首批代币接收地址后续的分配方式，核心不介入分配逻辑。
- **发射分配目标**：发射使用 `distributionTarget + distributionTargetMode`，并可携带不透明 `distributionKeys` / `distributionValues` KV；核心不解析接收方业务数据。
- **首个代币初始分配**：协议首次部署产生的首个代币也必须填写 `distributionTarget`。BSC 正式部署时该目标为旧 `LOVE20TKM/burn` 代码库部署的 `Airdrop` 合约，由其按 Merkle 快照完成初始分配；Burn 业务代码本身不迁入 `LOVE20BSC`。

## 阶段与轮次

- **Phase（阶段）**：底层阶段时间线中的一个无语义时间区间；投票、行动、验证、铸币等名称和组合由上层使用层定义。
- **Round（轮次）**：阶段时间线上的业务编号，从 `1` 开始；`0` 仅表示未设置，不是有效轮次。
- **阶段时间线**：由底层提供的无语义时间片、边界和动态区块参数；上层自行将时间片组合成轮次并赋予名称。
- **LOVE20Phase**：维护无语义阶段时间线及其历史记录、同步观测和动态校准的公共合约，不定义上层阶段名称或 `Round` 语义。

## 提案模型

- **Proposal（提案）**：底层治理框架的对象，由 `Submit` 创建、由 `Vote` 表决，并由 `Mint` 结算提案激励；底层使用 `proposalId`。
- **`proposalDetails`（提案详情）**：底层提案的通用详情字段，不带验证语义；在行动框架中，同一字段按行动语义称为 `verificationRule`，不重复存储。
- **`proposalTarget`（提案目标地址）**：提案激励接收地址，配合 `proposalTargetMode` 使用；`RewardOnly` 只接收激励，`Callback` 才触发回调。
- **提案回调**：`proposalTargetMode = Callback` 时，目标必须是合约；初始化 KV 或投票 KV 非空时才分别触发对应回调。`RewardOnly` 和空 KV 均不触发。

## 两层主架构

- **核心治理层**：由 `Stake`、`Submit`、`Vote` 和 `Mint` 组成，提供治理资产、提案基础数据、治理投票和奖励铸造等通用能力；`MemberNFT`、`LOVE20Phase` 和发射基础设施也属于核心层。治理激励只按核心治理规则结算，不依赖具体提案扩展的业务结果。
- **提案扩展层**：按提案类型实现不同的 `Target` 合约。当前社群行动类型使用 `ActionTarget`，负责该类型提案的初始化、映射和通用参与登记；其内部是否进一步拆分执行器、验证器或分配器，由该业务自行决定，不构成协议级架构层。
- **共享时间基础设施**：`LOVE20Phase` 为两层提供无语义阶段时间线、历史记录和动态校准，不定义业务阶段名称或轮次组合。

## 代码库分层

- **`core` 代码库**：维护第一层核心框架，包括底层治理框架、发射基础设施、`MemberNFT` 和 `LOVE20Phase`。发射资格与 `LOVE20LaunchNFT` 也属于这一层；`LOVE20TokenFactory` 保留为子币部署的技术拆分，用于规避组合后的合约体积或部署限制。
- **`launch` 代码库**：维护公平发射成功后的多种初始分配机制及配套合约；其中父币申购并非底层核心能力，而是 `launch` 中可选的一种分配机制。核心发射资格和 `LOVE20LaunchNFT` 仍由 `core` 维护。
- **`action` 代码库**：当前维护社群行动类型的 `ActionTarget` 及其内部业务组件；具体组件是否命名为 `ActionExecutor` 等由该业务决定。未来出现并列提案类型时，再按业务类型拆分新的代码库。

## 底层治理边界

- **投票回调**：`Vote` 不解析或理解提案或扩展业务，只将 `proposalId` 和治理者附带的 KV 资料交给 `proposalTarget` 处理；行动类提案由 `ActionTarget` 按自身业务规则转发或处理，回调失败回滚整笔投票交易。
- **投票回调触发条件**：只有 `proposalTargetMode = Callback` 且投票 KV 非空时才调用投票回调。
- **批量投票 KV**：多提案批量投票中每个 `proposalId` 可拥有独立 KV；提案扩展的私有字段由对应 `Target` 及其内部业务组件自行放入并解析，通用回调不包含特定业务字段。

## 通用行动边界

- **上层行动流程**：`ActionTarget` 提供该提案类型的通用参与登记和基础生命周期；行动类型内部自行把底层时间片组合成投票、行动、验证和铸币等阶段。
- **行动执行地址**：行动激励的唯一铸造授权地址，可以是合约或 EOA；零地址不改变该行动按规则获得的激励额度，只表示该额度在准备奖励时自动销毁，且不转给其他行动。
- **行动扩展标识**：行动类提案经 `ActionTarget` 初始化成功后，底层 `proposalId` 才可在该提案类型中作为有效 `actionId` 使用；普通提案没有 `actionId`。
- **行动验证信息**：行动类型将底层 `proposalDetails` 解释为 `verificationRule`；`verificationKeys`、`verificationKeyGuides` 等其余验证信息属于行动初始化 KV。`ActionTarget` 不替具体行动解释这些字段，由其内部业务组件自行保存和解释。
- **行动资产托管**：参与资产、扩展资产及其返还和行动内分配均由行动类型内部业务自行接收、记账和处理；`ActionTarget` 不接收、持有或返还行动资产。
- **体验资产**：体验资产按 `memberId + actionId + provider` 独立记账，资产归提供者所有，退出时返还对应提供者；同一 `MemberNFT` 的自有资产与体验资产互不抵扣。
- **部分撤回**：以行动自身的验证/激励快照为边界；快照前撤回影响当轮参与和激励，快照后撤回只影响后续轮次；余额归零后才清除行动参与登记。
- **正常行动流程**：加入、退出和资产取回均通过行动类型内部业务完成；前端首版只提供该提案类型定义的正常流程。
- **强制退出**：`forceExit` 仅是执行合约失效时的最后兜底，用于清理通用参与登记，绕过执行合约可能导致资产无法返还；前端默认不提供，首版不开放。
- **行动激励**：由行动执行地址按行动和轮次一次性领取，再由执行地址自行二次分配。

## 链群行动执行层

- **链群 owner**：链群 NFT（`groupId`）本身的业务主体，不是钱包地址；链群 NFT 在激活时就已确定，转移只改变控制者，不改变其身份和历史归属。
- **公共验证者候选人**：目标代币社区中拥有有效治理权的 `LOVE20Member`，以 `memberId` 参与每轮候选申请；不额外要求 SL/ST 或押金。
- **公共验证者竞选**：候选资料和公共验证者治理投票记录在链群行动执行合约；治理者在投票阶段独立于行动投票进行候选投票。
- **公共验证者申请生命周期**：候选申请在投票阶段可变更，变更会清空本轮投票；投票结束后当前轮候选、排名和分成比例冻结。
- **公共验证者验证资格**：链群行动执行合约初始化时传入验证阶段分割线数组；第 1 名默认在 `0%` 可提交，数组有 `n` 个严格递增分割线时，排名前 `n + 1` 名可验证；首个有效提交永久锁定本轮资格，不设置失效或接管。
- **验证阶段分割线**：数组允许为空；每个值必须满足 `0 < split < 1e18` 且严格递增，开放区块按本轮验证阶段起始区块和阶段区块数计算。
- **本轮验证集合**：行动阶段结束时至少有一个成员参与的链群 NFT 快照；空链群不计入，后续链群状态变化不影响本轮集合。
- **公共验证者**：按本轮候选排名获得提交资格并由首个有效批次锁定的唯一验证主体，负责完成全部快照链群验证。
- **公共验证者授权**：本轮锁定 `memberId` 而非钱包地址，每次批次提交要求该 `MemberNFT` 当前持有人直接调用；NFT 转移后由新持有人继续，不支持代理。
- **群级验证委托**：BSC 版不按 `groupId` 设置验证委托；`GroupVerify` 不保留 `setGroupDelegate`、`delegateByGroupId` 或基于群级 delegate 的验证授权，验证权限只来自本轮锁定的公共验证者 `memberId`。
- **公共验证者监督**：不保留不信任投票、理由、扣分或历史重算；治理者在下一轮投票阶段通过不给问题候选人投票来监督，不回溯修改上一轮奖励。
- **链群验证批次**：每个链群按本轮成员历史顺序维护连续游标；首个有效批次锁定整轮，必须完成全部快照链群验证。

## 群聊委托边界

- **Chat 专属 delegate**：delegate 只属于 `group-chat` 代码库，是群聊内部的管理和运营授权组件；`GroupChat`、`GroupAdmin`、`GroupMember`、`GroupBanList` 等 Chat 组件可以使用它。
- **禁止跨层授权**：Chat delegate 不进入 `core` 的通用身份或权限模型，不被 `action`、`launch` 或其他业务代码库作为权限来源，也不影响 `MemberNFT` 所有权、行动参与或公共验证者验证资格。
- **迁移实现**：不把旧 `group/src/GroupDelegate.sol` 作为全局合约迁入 `core`；BSC 版在 `group-chat` 内重写或迁入 Chat 所需的最小委托逻辑，具体实现名称在该代码库内确定（可采用 `GroupChatDelegate`）。

## 工厂边界

- **技术工厂例外**：`core` 保留 `LOVE20TokenFactory`，只负责按部署需要创建子币合约及其必要的核心配套，不负责创建行动或扩展执行合约；BSC 版移除旧 `SL/ST` 等已删除协议耦合，具体参数由发射票据确定。
- **业务工厂删除**：`action` 不保留按行动创建业务组件的工厂；旧 `Extension*Factory`、群行动工厂和 LP 扩展工厂均不迁移。
- **`group-chat` 当前部署模型**：一个 `GroupChat` 合约按 `groupId` 管理多个群，不按群部署独立 Chat 合约，因此当前不新增 `GroupChatFactory`；只有未来明确改为“一群一合约”时才重新评估。
- **外部 DEX 工厂**：PancakeSwap/Uniswap V2 Factory 属于外部依赖，只通过接口调用，不把实现作为 LOVE20 业务工厂迁入。

## 链群服务行动执行层

- **链群服务行动**：独立于链群行动的服务行动，其行动执行合约负责铸造和分配链群服务激励。
- **公共验证者服务费**：完整验证时，最终公共验证者按冻结的申请比例 `r` 获得链群服务激励总额的 `r`，其余按链群服务规则分给链群 owner；比例统一使用 `1e18` 精度。
- **链群验证激励结算**：公共验证者必须完成当轮全部链群验证才有激励；验证不完整时，该链群行动及其行动者、链群服务、公共验证者和链群 owner 奖励全部为 `0`，不影响独立的底层治理激励。

## 跨层约束

- **比例精度**：所有比例分配统一使用 `1e18` 精度，取值范围为 `0` 到 `1e18`。
- **当前轮次查询**：按层提供无参数的命名查询，不使用跨层共享的阶段枚举参数。
