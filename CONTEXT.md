# LOVE20BSC 治理术语

## 阶段与轮次

- **Phase（阶段）**：底层阶段时间线中的一个无语义时间区间；投票、行动、验证、铸币等名称和组合由上层使用层定义。
- **Round（轮次）**：阶段时间线上的业务编号，从 `1` 开始；`0` 仅表示未设置，不是有效轮次。
- **阶段时间线**：由底层提供的无语义时间片、边界和动态区块参数；上层自行将时间片组合成轮次并赋予名称。
- **LOVE20Phase**：维护无语义阶段时间线及其历史记录、同步观测和动态校准的公共合约，不定义上层阶段名称或 `Round` 语义。

## 治理层与行动层

- **底层治理框架**：由 `Stake`、`Submit`、`Vote` 和 `Mint` 组成，治理激励只涉及投票和铸造两个阶段。
- **上层行动流程**：由 `Join` 和行动执行地址承载，行动参与者经历投票、行动、验证和铸造四个阶段。
- **行动执行地址**：行动激励的唯一铸造授权地址，可以是合约或 EOA；零地址不改变该行动按规则获得的激励额度，只表示该额度在准备奖励时自动销毁，且不转给其他行动。
- **治理激励**：只依据 `Stake` 与 `Vote` 结算，不依赖行动验证结果。
- **行动激励**：由行动执行地址按行动和轮次一次性领取，再由执行地址自行二次分配。
- **公共验证者候选人**：目标代币社区中拥有有效治理权的 `LOVE20Member`，以 `memberId` 参与每轮候选申请；不额外要求 SL/ST 或押金。
- **公共验证者竞选与激励**：候选资料和治理投票记录在链群行动执行合约；验证者激励由链群服务行动执行合约铸造并分配，不从链群行动参与者激励中扣除。
- **比例精度**：所有比例分配统一使用 `1e18` 精度，取值范围为 `0` 到 `1e18`。
- **公共验证者申请生命周期**：候选申请在投票阶段可变更，变更会清空本轮投票；投票结束后当前轮候选、排名和分成比例冻结。
- **公共验证者验证资格**：链群行动执行合约初始化时传入验证阶段分割线数组；第 1 名默认在 `0%` 可提交，数组有 `n` 个严格递增分割线时，排名前 `n + 1` 名可验证；首个有效提交永久锁定本轮资格，不设置失效或接管。
- **公共验证者服务费**：完整验证时，最终验证者按冻结的申请比例 `r` 获得链群服务激励总额的 `r`，其余按链群服务规则分给链群主；比例统一使用 `1e18` 精度。
- **链群验证激励结算**：公共验证者必须完成当轮全部链群验证才有激励；验证不完整时，该链群行动及其行动者、链群服务、验证者和链群主奖励全部为 0，不影响独立的底层治理激励。
- **当前轮次查询**：按层提供无参数的命名查询，不使用跨层共享的阶段枚举参数。
