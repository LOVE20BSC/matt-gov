# Triage labels

Matt 技能使用以下固定标签。本仓库保留英文字符串，说明使用中文。

| Label | 含义 |
| --- | --- |
| `needs-triage` | 需要维护者评估 |
| `needs-info` | 等待补充信息 |
| `ready-for-agent` | 规格完整，可交给 AI agent |
| `ready-for-human` | 需要人工实施或判断 |
| `wontfix` | 本次不处理 |

每个 triage 项目应同时有一个类别标签（`bug` 或 `enhancement`）和一个状态标签。
